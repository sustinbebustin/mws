# journal/

Time-series session notes. One file per day (`YYYY-MM-DD.md`) or per significant session.

Cheap, optional context for the *next* session: what shipped today, what's still open, what's blocked, what surprised you. Better than reconstructing state from git log.

No required format. A few lines is fine.
